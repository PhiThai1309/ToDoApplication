<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/search"
        android:icon="@drawable/ic_baseline_search_24"
        android:title="Search"
        android:contentDescription="this is a search task button"
        app:showAsAction="ifRoom"
        app:actionViewClass="android.widget.SearchView" />

    <item
        android:id="@+id/sort"
        android:icon="@drawable/ic_baseline_filter_alt_24"
        android:title="Sort"
        android:contentDescription="this is a sort task button"
        app:showAsAction="ifRoom|collapseActionView"/>

    <item
        android:id="@+id/logOut"
        android:contentDescription="this is a log out button"
        android:icon="@drawable/ic_baseline_logout_24"
        android:title="Log Out"
        app:showAsAction="collapseActionView|withText" />
</menu>